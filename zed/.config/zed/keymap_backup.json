[
  {
    "context": "Editor && vim_mode == normal && vim_operator == none && !VimWaiting",
    "bindings": {
      "ctrl-b": "workspace::ToggleLeftDock", // Disable Ctrl+W in normal mode
      "ctrl-w": "pane::CloseActiveItem", // Disable Ctrl+W in normal mode
      "ctrl-pageup": "pane::ActivatePrevItem", // Disable Ctrl+W in normal mode
      "ctrl-pagedown": "pane::ActivateNextItem" // Disable Ctrl+W in normal mode
    }
  },
  {
      "context": "Terminal",
      "bindings": {
        "ctrl-w": "pane::CloseActiveItem"
      }
    }
]
